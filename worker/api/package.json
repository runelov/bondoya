{
  "name": "bondoya-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply bondoya --local",
    "db:migrate:remote": "wrangler d1 migrations apply bondoya --remote"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
